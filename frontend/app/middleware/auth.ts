// /middleware/auth.ts

export default defineNuxtRouteMiddleware((to, from) => {
  // const user = useAuthStore?.()?.user || null // adjust to your auth system

  // if (!user) {
  //   // Redirect to login page
  //   return navigateTo('/auth/login')
  // }
})
